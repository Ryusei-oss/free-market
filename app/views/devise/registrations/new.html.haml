.account-page__header
  %h1
    = link_to "#", class: "account-page__header__furima-logo__url" do
      = image_tag src="logo/logo.png", size: "185x49", class: "account-page__header__furima-logo__url__image"
    .account-page__header__process
      %ul.account-page__header__process__bar
        %li 会員情報
.account-page__main
  .account-page__main__text
    .account-page__main__text__header 
      会員情報入力画面
    .account-page__main__user-form
      = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| 
        .nickname
          .nickname__label
            ニックネーム
            %span 必須
            = f.text_field :nickname, placeholder: "例)フリマjapan"
        .email
          .email__label
            メール
            %span 必須
            = f.email_field :email, autofocus: true, autocomplete: "email", :placeholder => "PC/携帯どちらでも可"
        .email
          .email__label
            電話番号
            %span 必須
            = f.text_field :tell, autofocus: true, autocomplete: "tell", :placeholder => "PC/携帯どちらでも可"
        .password
          .password__label
            パスワード
            %span 必須
            %i (英数字8文字以上)
            = f.password_field :password, autocomplete: "new-password", :placeholder => "英数字8文字以上"
        .password-check
          .password-check__label
            パスワード確認
            %span 必須
            = f.password_field :password, autocomplete: "off", :placeholder => "入力ミスに注意してください"
        .identification
          %h3
            本人確認
          %p
            安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。他のお客さまに公開されることはありません。
        .name
          .name__label
            名前（全角）
            %span 必須
          = f.text_field :first_name, placeholder: "例）山田" , class: "name1"
          = f.text_field :last_name, placeholder: "例）太朗" , class: "name1"
        .kana
          .kana__label
            名前（カナ）
            %span 必須
          = f.text_field :first_name_kana, placeholder: "例） ヤマダ" , class:"kana1"
          = f.text_field :last_name_kana, placeholder: "例）タロウ" , class:"kana1"
        .birthday
          .birthday__label 
            生年月日
            %span 必須
          .choose
            .choose__year
              %i.icon
              = f.select :birthday, ['1990', '1991', '1992'], {select: '1990'}, class: "choose__year__textbox"
              %span 年
            .choose__month
              %i.icon
              //= f.select :birthday, ['1', '2', '12'], {select: '1'}, class: "choose__month__textbox"
              %span 月
            .choose__day
              %i.icon
              //= f.select :birthday, ['1', '2', '12'], {select: '1'}, class: "choose__day__textbox"
              %span 日
        %p.infomation
          ※ 本人情報は正しく入力してください。会員登録後、修正するにはお時間を頂く場合があります。
        .submit
          %button.submit1{:content => "text", :placeholder => "次へ進む"}
            次へ進む
          .personal-information
            %p
              = link_to "#", class: "personal-information1" do
                本人情報の登録について
      .form-group
        %p
          This site is protected by reCAPTCHA and the Google
          = link_to "#", class: "policy" do
            Privacy Policy 
          and
          = link_to "#", class: "service" do
            Terms of Service
          apply

.account-page__footer
  .account-page__footer__content
    %ul.footer__content__list
      %li
        = link_to "#", class: "hooter__content__list__privacy" do
          プライバシーポリシー
      %li
        = link_to "#", class: "hooter__content__list__terms" do
          利用規約
      %li
        = link_to "#", class: "hooter__content__list__law" do
          特定商品取引に関する表記
  .footer__logo
    = link_to "#", class: "hooter__logo__white" do
      = image_tag src="/assets/logo-white.png", size: "185x49", class: "hooter__logo__white__image"
    %p.footer__logo__copymark
      © Frema,Inc.

-# <!-- <%# <h2>Sign up</h2>

-# <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
-#   <%= render "devise/shared/error_messages", resource: resource %>

-#   <div class="field">
-#     <%= f.label :email %><br />
-#     <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
-#   </div>

-#   <div class="field">
-#     <%= f.label :password %>
-#     <% if @minimum_password_length %>
-#     <em>(<%= @minimum_password_length %> characters minimum)</em>
-#     <% end %><br />
-#     <%= f.password_field :password, autocomplete: "new-password" %>
-#   </div>

-#   <div class="field">
-#     <%= f.label :password_confirmation %><br />
-#     <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
-#   </div>

-#   <div class="actions">
-#     <%= f.submit "Sign up" %>
-#   </div>
-# <% end %>

-# <%= render "devise/shared/links" %> %> -->
